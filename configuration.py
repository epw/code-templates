# This is technically a very simple Python module, but the syntax is simple
# enough that it can be an effective configuration file without requiring
# any extra work.

name_placeholder = "$NAME"
template_dir = "/home/eric/opt/code-templates/"
extension = ".java"
